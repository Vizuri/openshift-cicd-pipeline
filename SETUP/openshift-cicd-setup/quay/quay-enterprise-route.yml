apiVersion: v1
kind: Route
metadata:
  name: quay-enterprise
spec:
  host: quay-enterprise-quay-enterprise.apps.ocpdemo.kee.vizuri.com
  port:
    targetPort: http
  to:
    kind: Service
    name: quay-enterprise
